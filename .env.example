# Esempio di file di configurazione per la produzione.
# Copia queste variabili e imposta i valori nel tuo servizio di hosting.

# --- Credenziali Amministratore ---
# Credenziali per l'accesso al pannello di amministrazione.
ADMIN_USERNAME=<il_tuo_username_admin>
ADMIN_PASSWORD=<la_tua_password_admin_complessa>

# --- Sicurezza JWT ---
# Una stringa segreta lunga, complessa e casuale per firmare i token.
JWT_SECRET=<la_tua_secret_key_molto_lunga_e_casuale>

# --- CORS ---
# Il dominio del tuo frontend per permettere le richieste API.
CORS_ALLOWED_ORIGINS=https://www.iltuodominio.it

# --- Credenziali Firebase ---
# Percorso assoluto sul server dove si trova il file delle credenziali di Firebase.
GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/firebase-credentials.json

# --- Configurazione Stripe (Chiavi LIVE) ---
# Chiave segreta di Stripe per processare i pagamenti reali.
STRIPE_SECRET_KEY=<la_tua_chiave_live_di_stripe_sk_live_...>

# --- Configurazione Cloudinary ---
# Credenziali per il servizio di hosting delle immagini.
CLOUDINARY_CLOUD_NAME=<il_tuo_cloud_name_da_cloudinary>
CLOUDINARY_API_KEY=<la_tua_api_key_da_cloudinary>
CLOUDINARY_API_SECRET=<il_tuo_api_secret_da_cloudinary>

# --- Configurazione Brevo (per invio email) ---
# Credenziali per il servizio di invio email transazionali.
BREVO_API_KEY=<la_tua_api_key_di_brevo>
BREVO_API_URL=https://api.brevo.com/v3/smtp/email
BREVO_SENDER_EMAIL=noreply@iltuodominio.it
